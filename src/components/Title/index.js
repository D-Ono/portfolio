import styled from "styled-components";

export const Title = styled.h1`
    color: var(--text-title);
    margin-bottom: 1.6rem;
    font-size: 3rem;
    display: inline-block;  
`;

export const TitleWrapper = styled.h1`
    color: var(--text-title-wrapper);
    margin-bottom: 1.6rem;
    font-size: 3rem;
    display: inline-block;  
`;